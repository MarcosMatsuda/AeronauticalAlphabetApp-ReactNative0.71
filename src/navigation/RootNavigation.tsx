import * as React from 'react';
import MainStackNavigation from './MainStackNavigation/MainStackNavigation';

const RootNavigation: React.FC = () => {
  return <MainStackNavigation />;
};

export default RootNavigation;
